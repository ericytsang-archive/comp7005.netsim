package gui.components

import javafx.scene.control.Label
import javafx.scene.control.ProgressBar

class ProgressDisplay
{
    val label:Label = Label()
    var progressBar:ProgressBar = ProgressBar(61.0)
}